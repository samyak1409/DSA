"""
https://leetcode.com/problems/maximize-amount-after-two-days-of-conversions
"""


def max_amount(initial_currency: str, pairs1: list[list[str]], rates1: list[float], pairs2: list[list[str]],
              rates2: list[float]) -> float:
    """"""

    # Hint 1: Choose an intermediate currency. Convert from initialCurrency to this currency on day 1, and from that
    # currency back to initialCurrency on day 2.
    # Hint 2: Use a DFS/BFS to calculate the direct conversion rate between any two currencies.

    # 1) Optimal (Graph): TC = O(); SC = O()

    pass
